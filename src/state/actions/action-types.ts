export enum DarkModeActionType {
  LIGHT = 'light',
  DARK = 'dark',
  TOGGLE = 'toggle',
}

export enum BankingActionType {
  DEPOSIT = 'deposit',
  WITHDRAW = 'withdraw',
  BANKRUPT = 'bankrupt',
}

export enum DatabaseActionType {
  CARDS = 'cards',
  DECKS = 'decks',
}
